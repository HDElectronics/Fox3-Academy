---
category: Controls
---

# Row

A horizontal run of controls that wraps on narrow screens.

Pure layout: no border, no label. Pass the controls as `items`.

```tsx
<Row items={[<Button key="a" label="Step" />, <Button key="b" label="Reset" variant="ghost" />]} />
```
